name: Test Composite Action

on:
  workflow_dispatch:

jobs:
  call-composite-action:
    name: Call composite action
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run composite action
        uses: amsrun/template/actions/test-action@main